<UserControl x:Class="SmartClassroom.GUI.Views.EnvironmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <StackPanel Margin="20">
        <TextBlock Text="Environment Dashboard"
                   FontSize="30" FontWeight="Bold"/>

        <TextBlock Text="{Binding Temperature, StringFormat='Temperature: {0}°C'}"
                   FontSize="18" Margin="5"/>

        <TextBlock Text="{Binding Humidity, StringFormat='Humidity: {0}%'}"
                   FontSize="18" Margin="5"/>

        <TextBlock Text="{Binding CO2, StringFormat='CO₂: {0} ppm'}"
                   FontSize="18" Margin="5"/>
    </StackPanel>

</UserControl>
